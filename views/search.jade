extends menu
block content
  section.main-section
    .row.max-width-X
      .row
        .large-12.large-centered.columns
          .row
            h1.title Employees Wiki
            p Welcome to Employees Wiki.
          .row.content
            -var msj =JSON.stringify(flashmsg);
            -if(msj!='[]'){
            p.text-info  #{msj}.
            -};
      .row
        .small-6.large-centered.columns
          form(action='/admin', method='post')
            .row.collapse
              label(for='inputSearch') Search by name or sername.
              .select2-custom
                input#e6.bigdrop( name='Search' placeholder='Name or Surname', value='null')

